function cfg = AppConfig()
    % Central Configuration for Resistor Scanner
    cfg.acquisition.processWidth = 800; % Resize width for speed
    cfg.segmentation.sensitivity = 0.6;
    cfg.display.verbose = true;
end
